<%= form_with(model: wine) do |form| %>
  <% if wine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wine.errors.count, "error") %> prohibited this wine from being saved:</h2>

      <ul>
        <% wine.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name,'Nombre del Vino'%>
    <%= form.text_field :name %>
  </div>

  
  <div id="assemblies">
    <%= form.fields_for :assemblies do |assembly|%>
    <%= render 'assembly_fields', f: assembly %>
  <%end %>
  <div class="links">
    <%= link_to_add_association "Agregar cepas", form, :assemblies %>
  </div>

  </div>

  <div class="actions">
    <%= form.submit 'Crear Vino',class:"btn btn-primary"%>
  </div>
<% end %>
