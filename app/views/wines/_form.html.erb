<%= form_with(model: wine) do |form| %>
  <% if wine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wine.errors.count, "error") %> prohibited this wine from being saved:</h2>

      <ul>
        <% wine.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name,'Nombre del Vino'%>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :strains, 'Cepas' %>
    <br>
    <%= form.collection_check_boxes(:strain_ids, @strains, :id, :name) do |b|%>
    <label class="form-check" type="radio"><%= b.label class:"label-check_box" do%></label>
      <%= b.check_box + b.text %>
  <% end %>
  <% end %>

  </div>
  

  <div class="actions">
    <%= form.submit 'Crear Vino',class:"btn btn-primary"%>
  </div>
<% end %>
